# ===== Ingestion EL (Supabase -> BigQuery) =====
polars~=1.0
adbc-driver-postgresql~=1.0
pyarrow~=17.0
google-cloud-bigquery~=3.20
python-dotenv~=1.0

# ===== Transformation =====
dbt-bigquery~=1.9

# ===== Orchestration (Dagster) =====
# Les 3 paquets doivent partager la même release (dagster-dbt suit la version de dagster).
dagster>=1.9
dagster-webserver>=1.9
dagster-dbt>=0.25
